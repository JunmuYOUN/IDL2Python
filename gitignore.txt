# 활성 로컬 설정 — 호스트/계정/사이트 경로가 들어가므로 커밋 금지 / local-only, never commit
**/config/env.yaml

# 실행 산출물·캐시 / run artifacts & caches
**/_cache/
**/_selftest_run/
**/_workspace/*
!**/_workspace/README.md

# probe/보고서 산출물 / probe & report outputs
**/*.sav
**/probe_*.npz
**/reports/parity/
**/reports/09_parity_certificate.md

# 에이전트 CLI 로컬 권한 / agent-CLI local permissions
**/settings.local.json

# Python
**/__pycache__/
*.pyc
.pytest_cache/
